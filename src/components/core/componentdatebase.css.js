const sheet = new CSSStyleSheet();
sheet.replace(`:host{
                    color: #fffff;
                    background-color: #00000;
                    font-family: sans-serif;
                    padding: 1rem 1rem 0;
                    display: block;
                    max-width: 21rem;
            }`
)

export default sheet;