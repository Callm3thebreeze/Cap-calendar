const sheet = new CSSStyleSheet();
sheet.replace(`:host{
                    color: #7FB2F0;
                    padding: 0 1rem 1rem;
                }
                :host(:hover){
                    color: #9A9EA1;
                }`
)

export default sheet